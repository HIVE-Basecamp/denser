# Confirm 3speak space with Meno

Type: task
Status: open
Blocked by: none

## Question

Bryan to confirm with Meno. Checklist: does the box run Docker; can it expose one hostname with wss:// (TLS); who restarts the container; any bandwidth cap; can we have a shell or only a deploy hook. Record the answers here.
